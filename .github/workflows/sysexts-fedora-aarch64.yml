name: "Build sysexts for Fedora (aarch64)"

env:
  GH_TOKEN: ${{ github.token }}
  ARCH: "aarch64"
  RELEASEURL: "https://github.com/travier/fedora-sysexts-exp/releases/download"

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  schedule:
    - cron:  '0 0 * * MON'
  workflow_dispatch:

# Needed to allow creating a release
permissions:
  contents: write

# Prevent multiple workflow runs from racing to ensure that pushes are made
# sequentialy for the main branch. Also cancel in progress workflow runs.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}-${{ inputs.sysext }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  build-fedora-41:
    runs-on: "ubuntu-24.04-arm"
    container:
      image: "ghcr.io/travier/fedora-sysexts-exp/buildroot:latest"
      options: "--privileged --security-opt label=disable --user 0:0"
    env:
      IMAGE: "quay.io/fedora-ostree-desktops/base-atomic:41"
    steps:
      - name: "Checkout repo"
        uses: actions/checkout@v4

      - name: "Setup artifacts and dnf cache directories"
        run: |
          mkdir -p artifacts .dnf-cache

      - name: "Mark directory as safe"
        run: |
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git fetch --no-tags --prune --depth=1 origin +refs/heads/main:refs/remotes/origin/main

      - name: "Build sysext: btop"
        uses: ./.github/actions/build
        with:
          sysext: 'btop'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: chromium"
        uses: ./.github/actions/build
        with:
          sysext: 'chromium'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: cilium-cli"
        uses: ./.github/actions/build
        with:
          sysext: 'cilium-cli'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: cloud-hypervisor"
        uses: ./.github/actions/build
        with:
          sysext: 'cloud-hypervisor'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: compsize"
        uses: ./.github/actions/build
        with:
          sysext: 'compsize'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: distrobox"
        uses: ./.github/actions/build
        with:
          sysext: 'distrobox'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: emacs"
        uses: ./.github/actions/build
        with:
          sysext: 'emacs'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: erofs-utils"
        uses: ./.github/actions/build
        with:
          sysext: 'erofs-utils'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: htop"
        uses: ./.github/actions/build
        with:
          sysext: 'htop'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: incus"
        uses: ./.github/actions/build
        with:
          sysext: 'incus'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: iwd"
        uses: ./.github/actions/build
        with:
          sysext: 'iwd'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: just"
        uses: ./.github/actions/build
        with:
          sysext: 'just'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: krb5-workstation"
        uses: ./.github/actions/build
        with:
          sysext: 'krb5-workstation'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: libvirtd-desktop"
        uses: ./.github/actions/build
        with:
          sysext: 'libvirtd-desktop'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: mullvad-vpn"
        uses: ./.github/actions/build
        with:
          sysext: 'mullvad-vpn'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: neovim"
        uses: ./.github/actions/build
        with:
          sysext: 'neovim'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: ripgrep"
        uses: ./.github/actions/build
        with:
          sysext: 'ripgrep'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: source-foundry-hack-fonts"
        uses: ./.github/actions/build
        with:
          sysext: 'source-foundry-hack-fonts'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: steam-devices"
        uses: ./.github/actions/build
        with:
          sysext: 'steam-devices'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: strace"
        uses: ./.github/actions/build
        with:
          sysext: 'strace'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: tree"
        uses: ./.github/actions/build
        with:
          sysext: 'tree'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: vim"
        uses: ./.github/actions/build
        with:
          sysext: 'vim'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: virtctl"
        uses: ./.github/actions/build
        with:
          sysext: 'virtctl'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: vscode"
        uses: ./.github/actions/build
        with:
          sysext: 'vscode'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: vscodium"
        uses: ./.github/actions/build
        with:
          sysext: 'vscodium'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: wasmtime"
        uses: ./.github/actions/build
        with:
          sysext: 'wasmtime'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: wireguard-tools"
        uses: ./.github/actions/build
        with:
          sysext: 'wireguard-tools'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: youki"
        uses: ./.github/actions/build
        with:
          sysext: 'youki'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: zoxide"
        uses: ./.github/actions/build
        with:
          sysext: 'zoxide'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: zsh"
        uses: ./.github/actions/build
        with:
          sysext: 'zsh'
          image: ${{ env.IMAGE }}

  build-fedora-42:
    runs-on: "ubuntu-24.04-arm"
    container:
      image: "ghcr.io/travier/fedora-sysexts-exp/buildroot:latest"
      options: "--privileged --security-opt label=disable --user 0:0"
    env:
      IMAGE: "quay.io/fedora-ostree-desktops/base-atomic:42"
    steps:
      - name: "Checkout repo"
        uses: actions/checkout@v4

      - name: "Setup artifacts and dnf cache directories"
        run: |
          mkdir -p artifacts .dnf-cache

      - name: "Mark directory as safe"
        run: |
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git fetch --no-tags --prune --depth=1 origin +refs/heads/main:refs/remotes/origin/main

      - name: "Build sysext: btop"
        uses: ./.github/actions/build
        with:
          sysext: 'btop'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: chromium"
        uses: ./.github/actions/build
        with:
          sysext: 'chromium'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: cilium-cli"
        uses: ./.github/actions/build
        with:
          sysext: 'cilium-cli'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: cloud-hypervisor"
        uses: ./.github/actions/build
        with:
          sysext: 'cloud-hypervisor'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: compsize"
        uses: ./.github/actions/build
        with:
          sysext: 'compsize'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: distrobox"
        uses: ./.github/actions/build
        with:
          sysext: 'distrobox'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: emacs"
        uses: ./.github/actions/build
        with:
          sysext: 'emacs'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: erofs-utils"
        uses: ./.github/actions/build
        with:
          sysext: 'erofs-utils'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: htop"
        uses: ./.github/actions/build
        with:
          sysext: 'htop'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: incus"
        uses: ./.github/actions/build
        with:
          sysext: 'incus'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: iwd"
        uses: ./.github/actions/build
        with:
          sysext: 'iwd'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: just"
        uses: ./.github/actions/build
        with:
          sysext: 'just'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: krb5-workstation"
        uses: ./.github/actions/build
        with:
          sysext: 'krb5-workstation'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: libvirtd-desktop"
        uses: ./.github/actions/build
        with:
          sysext: 'libvirtd-desktop'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: mullvad-vpn"
        uses: ./.github/actions/build
        with:
          sysext: 'mullvad-vpn'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: neovim"
        uses: ./.github/actions/build
        with:
          sysext: 'neovim'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: ripgrep"
        uses: ./.github/actions/build
        with:
          sysext: 'ripgrep'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: source-foundry-hack-fonts"
        uses: ./.github/actions/build
        with:
          sysext: 'source-foundry-hack-fonts'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: steam-devices"
        uses: ./.github/actions/build
        with:
          sysext: 'steam-devices'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: strace"
        uses: ./.github/actions/build
        with:
          sysext: 'strace'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: tree"
        uses: ./.github/actions/build
        with:
          sysext: 'tree'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: vim"
        uses: ./.github/actions/build
        with:
          sysext: 'vim'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: virtctl"
        uses: ./.github/actions/build
        with:
          sysext: 'virtctl'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: vscode"
        uses: ./.github/actions/build
        with:
          sysext: 'vscode'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: vscodium"
        uses: ./.github/actions/build
        with:
          sysext: 'vscodium'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: wasmtime"
        uses: ./.github/actions/build
        with:
          sysext: 'wasmtime'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: wireguard-tools"
        uses: ./.github/actions/build
        with:
          sysext: 'wireguard-tools'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: youki"
        uses: ./.github/actions/build
        with:
          sysext: 'youki'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: zoxide"
        uses: ./.github/actions/build
        with:
          sysext: 'zoxide'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: zsh"
        uses: ./.github/actions/build
        with:
          sysext: 'zsh'
          image: ${{ env.IMAGE }}

  build-fedora-coreos-stable:
    runs-on: "ubuntu-24.04-arm"
    container:
      image: "ghcr.io/travier/fedora-sysexts-exp/buildroot:latest"
      options: "--privileged --security-opt label=disable --user 0:0"
    env:
      IMAGE: "quay.io/fedora/fedora-coreos:stable"
    steps:
      - name: "Checkout repo"
        uses: actions/checkout@v4

      - name: "Setup artifacts and dnf cache directories"
        run: |
          mkdir -p artifacts .dnf-cache

      - name: "Mark directory as safe"
        run: |
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git fetch --no-tags --prune --depth=1 origin +refs/heads/main:refs/remotes/origin/main

      - name: "Build sysext: gdb"
        uses: ./.github/actions/build
        with:
          sysext: 'gdb'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: kubernetes-1.29"
        uses: ./.github/actions/build
        with:
          sysext: 'kubernetes-1.29'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: kubernetes-1.30"
        uses: ./.github/actions/build
        with:
          sysext: 'kubernetes-1.30'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: kubernetes-1.31"
        uses: ./.github/actions/build
        with:
          sysext: 'kubernetes-1.31'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: kubernetes-1.32"
        uses: ./.github/actions/build
        with:
          sysext: 'kubernetes-1.32'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: kubernetes-cri-o-1.29"
        uses: ./.github/actions/build
        with:
          sysext: 'kubernetes-cri-o-1.29'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: kubernetes-cri-o-1.30"
        uses: ./.github/actions/build
        with:
          sysext: 'kubernetes-cri-o-1.30'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: kubernetes-cri-o-1.31"
        uses: ./.github/actions/build
        with:
          sysext: 'kubernetes-cri-o-1.31'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: kubernetes-cri-o-1.32"
        uses: ./.github/actions/build
        with:
          sysext: 'kubernetes-cri-o-1.32'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: python3"
        uses: ./.github/actions/build
        with:
          sysext: 'python3'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: semanage"
        uses: ./.github/actions/build
        with:
          sysext: 'semanage'
          image: ${{ env.IMAGE }}

  build-fedora-coreos-next:
    runs-on: "ubuntu-24.04-arm"
    container:
      image: "ghcr.io/travier/fedora-sysexts-exp/buildroot:latest"
      options: "--privileged --security-opt label=disable --user 0:0"
    env:
      IMAGE: "quay.io/fedora/fedora-coreos:next"
    steps:
      - name: "Checkout repo"
        uses: actions/checkout@v4

      - name: "Setup artifacts and dnf cache directories"
        run: |
          mkdir -p artifacts .dnf-cache

      - name: "Mark directory as safe"
        run: |
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git fetch --no-tags --prune --depth=1 origin +refs/heads/main:refs/remotes/origin/main

      - name: "Build sysext: gdb"
        uses: ./.github/actions/build
        with:
          sysext: 'gdb'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: kubernetes-1.29"
        uses: ./.github/actions/build
        with:
          sysext: 'kubernetes-1.29'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: kubernetes-1.30"
        uses: ./.github/actions/build
        with:
          sysext: 'kubernetes-1.30'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: kubernetes-1.31"
        uses: ./.github/actions/build
        with:
          sysext: 'kubernetes-1.31'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: kubernetes-1.32"
        uses: ./.github/actions/build
        with:
          sysext: 'kubernetes-1.32'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: kubernetes-cri-o-1.29"
        uses: ./.github/actions/build
        with:
          sysext: 'kubernetes-cri-o-1.29'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: kubernetes-cri-o-1.30"
        uses: ./.github/actions/build
        with:
          sysext: 'kubernetes-cri-o-1.30'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: kubernetes-cri-o-1.31"
        uses: ./.github/actions/build
        with:
          sysext: 'kubernetes-cri-o-1.31'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: kubernetes-cri-o-1.32"
        uses: ./.github/actions/build
        with:
          sysext: 'kubernetes-cri-o-1.32'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: python3"
        uses: ./.github/actions/build
        with:
          sysext: 'python3'
          image: ${{ env.IMAGE }}

      - name: "Build sysext: semanage"
        uses: ./.github/actions/build
        with:
          sysext: 'semanage'
          image: ${{ env.IMAGE }}

  publish-sysexts:
    runs-on: "ubuntu-24.04-arm"
    container:
      image: "ghcr.io/travier/fedora-sysexts-exp/buildroot:latest"
      options: "--privileged --security-opt label=disable --user 0:0"
    # Run after the builds and versionned release
    needs:
      - build-fedora-41
      - build-fedora-42
      - build-fedora-coreos-stable
      - build-fedora-coreos-next
    # Still run if any dependent job fails
    # if: ${{ always() }}
    if: (github.event_name == 'push' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') && github.ref == 'refs/heads/main'
    steps:
      - name: "Checkout repo"
        uses: actions/checkout@v4

      - name: "Mark directory as safe"
        run: |
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git fetch --no-tags --prune --depth=1 origin +refs/heads/main:refs/remotes/origin/main

      - name: "Gather versions for sysext: btop"
        uses: ./.github/actions/gather
        with:
          sysext: 'btop'

      - name: "Gather versions for sysext: chromium"
        uses: ./.github/actions/gather
        with:
          sysext: 'chromium'

      - name: "Gather versions for sysext: cilium-cli"
        uses: ./.github/actions/gather
        with:
          sysext: 'cilium-cli'

      - name: "Gather versions for sysext: cloud-hypervisor"
        uses: ./.github/actions/gather
        with:
          sysext: 'cloud-hypervisor'

      - name: "Gather versions for sysext: compsize"
        uses: ./.github/actions/gather
        with:
          sysext: 'compsize'

      - name: "Gather versions for sysext: distrobox"
        uses: ./.github/actions/gather
        with:
          sysext: 'distrobox'

      - name: "Gather versions for sysext: emacs"
        uses: ./.github/actions/gather
        with:
          sysext: 'emacs'

      - name: "Gather versions for sysext: erofs-utils"
        uses: ./.github/actions/gather
        with:
          sysext: 'erofs-utils'

      - name: "Gather versions for sysext: htop"
        uses: ./.github/actions/gather
        with:
          sysext: 'htop'

      - name: "Gather versions for sysext: incus"
        uses: ./.github/actions/gather
        with:
          sysext: 'incus'

      - name: "Gather versions for sysext: iwd"
        uses: ./.github/actions/gather
        with:
          sysext: 'iwd'

      - name: "Gather versions for sysext: just"
        uses: ./.github/actions/gather
        with:
          sysext: 'just'

      - name: "Gather versions for sysext: krb5-workstation"
        uses: ./.github/actions/gather
        with:
          sysext: 'krb5-workstation'

      - name: "Gather versions for sysext: libvirtd-desktop"
        uses: ./.github/actions/gather
        with:
          sysext: 'libvirtd-desktop'

      - name: "Gather versions for sysext: mullvad-vpn"
        uses: ./.github/actions/gather
        with:
          sysext: 'mullvad-vpn'

      - name: "Gather versions for sysext: neovim"
        uses: ./.github/actions/gather
        with:
          sysext: 'neovim'

      - name: "Gather versions for sysext: ripgrep"
        uses: ./.github/actions/gather
        with:
          sysext: 'ripgrep'

      - name: "Gather versions for sysext: source-foundry-hack-fonts"
        uses: ./.github/actions/gather
        with:
          sysext: 'source-foundry-hack-fonts'

      - name: "Gather versions for sysext: steam-devices"
        uses: ./.github/actions/gather
        with:
          sysext: 'steam-devices'

      - name: "Gather versions for sysext: strace"
        uses: ./.github/actions/gather
        with:
          sysext: 'strace'

      - name: "Gather versions for sysext: tree"
        uses: ./.github/actions/gather
        with:
          sysext: 'tree'

      - name: "Gather versions for sysext: vim"
        uses: ./.github/actions/gather
        with:
          sysext: 'vim'

      - name: "Gather versions for sysext: virtctl"
        uses: ./.github/actions/gather
        with:
          sysext: 'virtctl'

      - name: "Gather versions for sysext: vscode"
        uses: ./.github/actions/gather
        with:
          sysext: 'vscode'

      - name: "Gather versions for sysext: vscodium"
        uses: ./.github/actions/gather
        with:
          sysext: 'vscodium'

      - name: "Gather versions for sysext: wasmtime"
        uses: ./.github/actions/gather
        with:
          sysext: 'wasmtime'

      - name: "Gather versions for sysext: wireguard-tools"
        uses: ./.github/actions/gather
        with:
          sysext: 'wireguard-tools'

      - name: "Gather versions for sysext: youki"
        uses: ./.github/actions/gather
        with:
          sysext: 'youki'

      - name: "Gather versions for sysext: zoxide"
        uses: ./.github/actions/gather
        with:
          sysext: 'zoxide'

      - name: "Gather versions for sysext: zsh"
        uses: ./.github/actions/gather
        with:
          sysext: 'zsh'


      - name: "Gather versions for sysext: btop"
        uses: ./.github/actions/gather
        with:
          sysext: 'btop'

      - name: "Gather versions for sysext: chromium"
        uses: ./.github/actions/gather
        with:
          sysext: 'chromium'

      - name: "Gather versions for sysext: cilium-cli"
        uses: ./.github/actions/gather
        with:
          sysext: 'cilium-cli'

      - name: "Gather versions for sysext: cloud-hypervisor"
        uses: ./.github/actions/gather
        with:
          sysext: 'cloud-hypervisor'

      - name: "Gather versions for sysext: compsize"
        uses: ./.github/actions/gather
        with:
          sysext: 'compsize'

      - name: "Gather versions for sysext: distrobox"
        uses: ./.github/actions/gather
        with:
          sysext: 'distrobox'

      - name: "Gather versions for sysext: emacs"
        uses: ./.github/actions/gather
        with:
          sysext: 'emacs'

      - name: "Gather versions for sysext: erofs-utils"
        uses: ./.github/actions/gather
        with:
          sysext: 'erofs-utils'

      - name: "Gather versions for sysext: htop"
        uses: ./.github/actions/gather
        with:
          sysext: 'htop'

      - name: "Gather versions for sysext: incus"
        uses: ./.github/actions/gather
        with:
          sysext: 'incus'

      - name: "Gather versions for sysext: iwd"
        uses: ./.github/actions/gather
        with:
          sysext: 'iwd'

      - name: "Gather versions for sysext: just"
        uses: ./.github/actions/gather
        with:
          sysext: 'just'

      - name: "Gather versions for sysext: krb5-workstation"
        uses: ./.github/actions/gather
        with:
          sysext: 'krb5-workstation'

      - name: "Gather versions for sysext: libvirtd-desktop"
        uses: ./.github/actions/gather
        with:
          sysext: 'libvirtd-desktop'

      - name: "Gather versions for sysext: mullvad-vpn"
        uses: ./.github/actions/gather
        with:
          sysext: 'mullvad-vpn'

      - name: "Gather versions for sysext: neovim"
        uses: ./.github/actions/gather
        with:
          sysext: 'neovim'

      - name: "Gather versions for sysext: ripgrep"
        uses: ./.github/actions/gather
        with:
          sysext: 'ripgrep'

      - name: "Gather versions for sysext: source-foundry-hack-fonts"
        uses: ./.github/actions/gather
        with:
          sysext: 'source-foundry-hack-fonts'

      - name: "Gather versions for sysext: steam-devices"
        uses: ./.github/actions/gather
        with:
          sysext: 'steam-devices'

      - name: "Gather versions for sysext: strace"
        uses: ./.github/actions/gather
        with:
          sysext: 'strace'

      - name: "Gather versions for sysext: tree"
        uses: ./.github/actions/gather
        with:
          sysext: 'tree'

      - name: "Gather versions for sysext: vim"
        uses: ./.github/actions/gather
        with:
          sysext: 'vim'

      - name: "Gather versions for sysext: virtctl"
        uses: ./.github/actions/gather
        with:
          sysext: 'virtctl'

      - name: "Gather versions for sysext: vscode"
        uses: ./.github/actions/gather
        with:
          sysext: 'vscode'

      - name: "Gather versions for sysext: vscodium"
        uses: ./.github/actions/gather
        with:
          sysext: 'vscodium'

      - name: "Gather versions for sysext: wasmtime"
        uses: ./.github/actions/gather
        with:
          sysext: 'wasmtime'

      - name: "Gather versions for sysext: wireguard-tools"
        uses: ./.github/actions/gather
        with:
          sysext: 'wireguard-tools'

      - name: "Gather versions for sysext: youki"
        uses: ./.github/actions/gather
        with:
          sysext: 'youki'

      - name: "Gather versions for sysext: zoxide"
        uses: ./.github/actions/gather
        with:
          sysext: 'zoxide'

      - name: "Gather versions for sysext: zsh"
        uses: ./.github/actions/gather
        with:
          sysext: 'zsh'


      - name: "Gather versions for sysext: gdb"
        uses: ./.github/actions/gather
        with:
          sysext: 'gdb'

      - name: "Gather versions for sysext: kubernetes-1.29"
        uses: ./.github/actions/gather
        with:
          sysext: 'kubernetes-1.29'

      - name: "Gather versions for sysext: kubernetes-1.30"
        uses: ./.github/actions/gather
        with:
          sysext: 'kubernetes-1.30'

      - name: "Gather versions for sysext: kubernetes-1.31"
        uses: ./.github/actions/gather
        with:
          sysext: 'kubernetes-1.31'

      - name: "Gather versions for sysext: kubernetes-1.32"
        uses: ./.github/actions/gather
        with:
          sysext: 'kubernetes-1.32'

      - name: "Gather versions for sysext: kubernetes-cri-o-1.29"
        uses: ./.github/actions/gather
        with:
          sysext: 'kubernetes-cri-o-1.29'

      - name: "Gather versions for sysext: kubernetes-cri-o-1.30"
        uses: ./.github/actions/gather
        with:
          sysext: 'kubernetes-cri-o-1.30'

      - name: "Gather versions for sysext: kubernetes-cri-o-1.31"
        uses: ./.github/actions/gather
        with:
          sysext: 'kubernetes-cri-o-1.31'

      - name: "Gather versions for sysext: kubernetes-cri-o-1.32"
        uses: ./.github/actions/gather
        with:
          sysext: 'kubernetes-cri-o-1.32'

      - name: "Gather versions for sysext: python3"
        uses: ./.github/actions/gather
        with:
          sysext: 'python3'

      - name: "Gather versions for sysext: semanage"
        uses: ./.github/actions/gather
        with:
          sysext: 'semanage'


      - name: "Gather versions for sysext: gdb"
        uses: ./.github/actions/gather
        with:
          sysext: 'gdb'

      - name: "Gather versions for sysext: kubernetes-1.29"
        uses: ./.github/actions/gather
        with:
          sysext: 'kubernetes-1.29'

      - name: "Gather versions for sysext: kubernetes-1.30"
        uses: ./.github/actions/gather
        with:
          sysext: 'kubernetes-1.30'

      - name: "Gather versions for sysext: kubernetes-1.31"
        uses: ./.github/actions/gather
        with:
          sysext: 'kubernetes-1.31'

      - name: "Gather versions for sysext: kubernetes-1.32"
        uses: ./.github/actions/gather
        with:
          sysext: 'kubernetes-1.32'

      - name: "Gather versions for sysext: kubernetes-cri-o-1.29"
        uses: ./.github/actions/gather
        with:
          sysext: 'kubernetes-cri-o-1.29'

      - name: "Gather versions for sysext: kubernetes-cri-o-1.30"
        uses: ./.github/actions/gather
        with:
          sysext: 'kubernetes-cri-o-1.30'

      - name: "Gather versions for sysext: kubernetes-cri-o-1.31"
        uses: ./.github/actions/gather
        with:
          sysext: 'kubernetes-cri-o-1.31'

      - name: "Gather versions for sysext: kubernetes-cri-o-1.32"
        uses: ./.github/actions/gather
        with:
          sysext: 'kubernetes-cri-o-1.32'

      - name: "Gather versions for sysext: python3"
        uses: ./.github/actions/gather
        with:
          sysext: 'python3'

      - name: "Gather versions for sysext: semanage"
        uses: ./.github/actions/gather
        with:
          sysext: 'semanage'

      - name: "Update latest release for all sysexts"
        uses: ./.github/actions/latest
